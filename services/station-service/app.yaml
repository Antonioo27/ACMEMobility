kind: Service
apiVersion: v1
metadata:
  name: station-service
  labels:
    app: station-service
spec:
  type: ClusterIP
  selector:
    app: station-service
  ports:
    - name: tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: station-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: station-service
  template:
    metadata:
      labels:
        app: station-service
        version: v1
    spec:
      containers:
      - name: station-service
        image: station-service
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
